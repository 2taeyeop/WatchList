# CLAUDE.md

> 범용 협업 지침. 새 프로젝트 루트에 복사한 뒤 **`«채울 것»`** 부분만 그 프로젝트에 맞게 채운다.
> 나머지(작업 방식·코드 스타일·보안)는 프로젝트와 무관하게 그대로 적용한다.

## 프로젝트

- **이름 / 설명**: WatchList — QLD/JEPI 규칙서 집행봇. 텔레그램으로 잔고 스크린샷을 받아 규칙서(`prompts/rulebook.md`)대로 판정·주문표·로그를 회신한다. 조언자가 아니라 집행자 — 뉴스·전망·추천은 금지된 입력.
- **스택 / 런타임 버전**: Python 3.12 (requests·yfinance·pandas·pytest), SQLite, Claude Code CLI(`claude -p` 구독 인증 — 이미지에 node+claude 포함), 배포 EC2 공용 서버 Docker 컨테이너 1개(롱폴링 — 공개 포트 없음) + 리마인더는 호스트 cron.
- **로컬 경로**: 백엔드 전부 `backend/` (봇 `bot.py` · 판정 엔진 `rules.py` · 리마인더 `jobs/reminder.py`), 규칙서 `prompts/`, 테스트 `tests/`.
- **주요 명령어**:
  - 설치: `pip install -r requirements.txt`
  - 테스트: `python -m pytest tests/ -q`
  - 봇 실행: `python -m backend.bot` · 리마인더: `python -m backend.jobs.reminder`
- **핵심 원칙**: 돈 계산(판정·주수)은 `rules.py` 순수 함수 + 유닛테스트로만. 클로드는 스크린샷 추출(`vision.py`)과 자유 질문(`reply.py`)에만 쓴다. 시장 데이터는 서버가 yfinance로 직접 조회(클로드 웹검색 의존 금지).

### 직접 실행하지 말 것

- **비밀값은 `.env`/GitHub Secrets로만** — 코드·커밋·로그에 키를 넣지 말 것.
- 배포/빌드 명령은 EC2·GitHub Actions에서 돌아감 — 로컬에서 함부로 실행 금지.

## 커뮤니케이션

- **한국어로 답한다.**
- 결정이 필요한 갈림길에선 **옵션 A/B/C를 장단점과 함께 제시하고 추천 1개를 분명히** 밝힌 뒤 내 답을 기다린다. 단, 자명한 기본값이 있으면 그냥 진행하고 한 줄로 알린다 — 사소한 선택을 일일이 묻지 않는다.
- 행동할 만큼 정보가 모이면 **행동한다.** 이미 정한 결정을 다시 꺼내거나, 추진하지 않을 선택지를 장황히 나열하지 않는다.
- **사실대로 보고한다**: 끝났으면 끝났다고, 실패했으면 출력과 함께 실패했다고, 건너뛰었으면 건너뛰었다고. 검증 안 한 걸 "됐다"고 말하지 않는다.

## 작업 흐름

### 커밋 (가장 중요)

- **커밋은 내가 "커밋해"라고 명시할 때만 한다.** 작업이 끝나도 자동으로 `git commit` 하지 않는다.
- **push는 항상 내가 직접 한다.**
- 기본 브랜치(main 등)에 직접 커밋하지 말고, 필요하면 먼저 브랜치를 판다.
- **변경 묶음이 끝나면 요청하지 않아도 커밋 메시지 후보를 제시한다** — 제목 한 줄 + 본문 불릿(무엇을·왜).
- 커밋 메시지와 함께 **테스트 안내**도 같이 준다: 진입 경로(어느 화면/버튼), 확인 포인트, 네트워크 호출 검증 포인트.

### 큰 작업은 plan 먼저

- 여러 파일·여러 단계·결정이 필요한 작업은 **코드를 건드리기 전에 plan 문서**(`docs/plans/` 등)로 추적한다. Context · 결정사항(옵션+추천) · 변경 대상 · 구현 순서 · 검증 · 비범위를 적는다.
- 작은 단발 작업(파일 1~2개, 결정 불필요)엔 plan을 만들지 않는다 — 과한 절차는 방해다.
- 구현은 **점진적·검증 가능한 단계**(Step 0, 1, 2 …)로 쪼개고, 각 단계 뒤 검증한다.

### 검증 (끝났다 말하기 전에)

- 변경 후 **import 해소 · 빌드 깨짐 없음 · 영향 범위 격리**를 확인한다.
- 한 모듈을 고친 뒤 **다른 모듈에 영향이 없는지** 점검한다 (특히 동명 함수·전역 vs 로컬 충돌).
- **기계적 대량 변경**(파일 이동, import 재작성, 일괄 치환)은 손으로 하지 말고 **결정적 스크립트**로 처리한 뒤 결과를 전수 검증한다.
  - import는 `from "…"` · 동적 `import("…")` · **bare side-effect `import "…"`** 세 형태를 모두 처리·검증한다 (bare 형태가 가장 놓치기 쉬움).
  - 파일 이동은 `git mv`로 해 rename 이력을 보존한다.

## 코드 스타일

- **기존 코드에 맞춘다.** 주변 코드의 주석 밀도·네이밍·패턴을 읽고 그대로 따른다. 내 취향을 새로 들이지 않는다.
- "**X처럼 정리/리팩토링해줘**"는 X의 폴더 구조·네이밍·분리 패턴을 **그대로 복제**하라는 뜻이다.
- **주석은 한글, 식별자는 영문.** 주석은 WHAT 말고 **WHY**(의도·제약)만 짧게.
- **early return**으로 분기 깊이를 줄인다.
- **재구현 전에 기존 공용 유틸/컴포넌트/상수를 먼저 찾는다.** 같은 걸 다시 만들지 않는다.
- **죽은 코드는 사용처를 확인한 뒤 삭제한다.** 안 쓰는 export·파일·분기를 남기지 않는다.
- **하드코딩 금지** — 색상·설정값·문자열은 프로젝트의 변수/상수/토큰을 쓴다.
- 내가 **정확한 값(크기·색·동작)을 지정하면 근사치로 때우지 말고 그대로** 반영한다.
- 새 스타일/패턴을 도입하기 전에 프로젝트가 이미 정한 방식(스타일링·상태관리 등)을 따른다.

## 파일·폴더 구조

### 원칙 (프로젝트 무관)

- **디렉토리마다 역할이 하나로 분명해야 한다.** "이게 왜 여기 있지?" 싶은 모호한 위치를 만들지 않는다. 둘 곳이 애매하면 먼저 기존 구조의 역할 분담을 분석하고 가장 맞는 곳을 고른다(없으면 새 위치를 제안).
- **기능(화면/도메인) 단위로 co-locate** 한다. 한 기능의 컴포넌트·hook·스타일·유틸·하위 컴포넌트는 그 기능 폴더 안에 모은다. 흩어 두지 않는다. (예: `«Feature»/«Feature».«ext»` + `«Feature».style` + `hooks/` + 하위 `components/`)
- **형제끼리 구조를 통일한다.** 한 기능을 어떤 패턴으로 정리했으면 같은 층위의 다른 기능도 같은 패턴으로 맞춘다 — "**X처럼 정리해줘**"의 핵심.
- **하위가 많아지면 역할별로 한 단계 더 묶는다** (`components/` · `modals/` · `hooks/` · `api/` · `utils/`). 라우트/페이지가 여럿인 기능은 **페이지를 상위 폴더**로 잡는다 (예: `list/` · `detail/`).
- **진짜 공유되는 것만 공유 레이어로.** 두 기능이 같은 자산을 쓰면 한쪽이 다른 쪽을 가져다 쓰지(cross-import) 말고, **도메인-독립 공용 레이어**(`shared/` 등)로 올린 뒤 양쪽이 거기서 가져온다.
- **묶음이 1~2개면 단일 폴더, 여러 개면 소문자 그룹 폴더**로 묶는다 (예: 인증 화면 4종 → `auth/`).
- **배럴(index) 파일은 프로젝트 합의에 따른다** — 쓰기로 했으면 일관되게, 안 쓰기로 했으면 직접 경로로 import. 섞지 않는다.
- 파일을 옮기면 import를 전부 갱신하고 **0 broken을 검증**한다. (위 "검증" 참조)

### 이 프로젝트의 구조

| 위치              | 용도                                                                     |
| ----------------- | ------------------------------------------------------------------------ |
| `backend/`        | 봇(`bot.py`)·판정 엔진(`rules.py`)·시장 데이터(`market.py`)·추출(`vision.py`)·회신(`reply.py`)·저장소(`db.py`) |
| `backend/jobs/`   | 배치 잡(`reminder.py` — 매일 아침 리마인더)                              |
| `prompts/`        | 투자 규칙서(`rulebook.md`) — 개정 시 이 파일 갱신 = 봇 지식 갱신         |
| `tests/`          | pytest 유닛·시나리오 테스트(돈 계산 검증)                                |
| `deploy/`         | `Dockerfile` + `docker-compose.yml`(bot 컨테이너) — 리마인더는 호스트 cron |
| `docs/plans/`     | 작업 plan 문서                                                           |
| `data/`           | SQLite 파일(런타임, .gitignore 제외)                                     |

### 공용 자산 빠른 참조

재구현 전에 먼저 여기부터 확인할 것:

| 자산                                   | 위치                       |
| -------------------------------------- | -------------------------- |
| 판정·주수 계산(순수 함수·규칙 상수)    | `backend/rules.py`         |
| DB 스키마·state/로그/pending 헬퍼      | `backend/db.py`            |
| 텔레그램 Bot API 클라이언트            | `backend/notify.py`        |
| claude -p 헤드리스 호출(구독 인증 강제) | `backend/claude_runner.py` |
| 시장 데이터(^NDX·현재가·환율)          | `backend/market.py`        |
| 회신 포맷·위기 배너·가드레일 프롬프트  | `backend/reply.py`         |

## 외부 계약은 추측하지 말고 검증

- 백엔드 응답·API 스키마·외부 라이브러리 구조는 **추측하지 말고 실제 소스(DTO·스키마·타입 정의)를 직접 읽어** 필드 이름·타입·함정을 확인한 뒤 코드를 쓴다.

## 보안

- `.env` · 자격증명 · 키스토어(`*.keystore`/`*.jks`/`*.p12`/`*.pem`) · 서비스 계정 JSON 등 **비밀 파일은 읽지도, 출력(echo/cat)하지도, 커밋하지도 않는다.**
- 자격증명 디렉토리는 건드리지 않는다.
- 외부로 데이터를 보내는 명령(curl·wget 등)은 쓰지 않는다.
- 되돌리기 어렵거나 외부로 나가는 작업은 명시적 승인 전엔 실행하지 않는다.

## 메모리 / 반복 지시

- 반복되는 **비자명한 교훈**(이 프로젝트만의 함정, 내가 준 작업 방식 피드백)은 메모리에 남겨 다음 세션이 이어받게 한다. 코드에 이미 적힌 것(구조·히스토리)은 남기지 않는다.
- "**앞으로 항상/매번 X**" 같은 자동 동작은 메모리가 아니라 **hook(settings.json)** 으로 처리해야 실제로 동작한다.
